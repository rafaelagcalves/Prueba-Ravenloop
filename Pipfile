[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
flask = ">=1.1.*"
flask-sqlalchemy = ">=2.4.*"
flask-migrate = ">=2.7.*"

[dev-packages]

[requires]
python_version = "3.6"

